{{ form_start(form) }}
    {{ form_widget(form) }}

<div class="form-actions" style="margin-top: 2rem; text-align: right;">
    <button class="btn btn-primary btn-save">
        <i class="fas fa-save"></i> {{ button_label|default('Salvar Local') }}
    </button>
</div>
{{ form_end(form) }}
