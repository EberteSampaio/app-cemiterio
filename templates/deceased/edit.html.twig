{% extends 'base.html.twig' %}

{% block title %}Editar Falecido{% endblock %}

{% block body %}
    <main class="main">
        <section class="about section">

            <div class="section-header">
                <h1 class="section-title">Editar Registro</h1>
                <p class="section-subtitle">Atualize as informações do falecido abaixo.</p>
            </div>

            <div class="form-card">
                {{ include('deceased/_form.html.twig', {'button_label': 'Atualizar'}) }}
            </div>

            <div class="actions">
                <a href="{{ path('app_deceased_index') }}" class="btn btn-secondary">
                    Voltar à Lista
                </a>

                {{ include('deceased/_delete_form.html.twig') }}
            </div>

        </section>
    </main>
{% endblock %}
